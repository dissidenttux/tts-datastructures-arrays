package com.company;

public class Main {

    public static void main(String[] args) {
        // write your code here
        /*
        1. Write a program to sum all of the values of a given array in Java.

        2. What is the output?
          double[ ] exampleArray = {1,5,6,5,4,1};
          double maximum = examplesArray[0];
          int index = 0;
          for (int i = 1; i<exampleArray.length>; i++){
               if (exampleArray[ i ] > maximum) {
                    maximum = exampleArray[ i ];
                    index = i;
               }
          }
          System.out.println(index);

        3. Write a public static method called "toPower" that takes in as parameters two
        integers called size and power.
        The method should return an array of size "size" with each array index raised to the
        value of "power." So, for example, if we passed in "size = 4" and
        "power = 2" to the "toPower," the method should return the following result: [0,1,4,9].
         */
        /*-----------------*/

        // PROBLEM 1 - ARRAY

        // Declare array
//        int[] numArray = {2,3,4,5,7,8};
//
//        // sum all of the values of the given array
//        // here, we're initializing a variable to append a "total" value to later
//        int total = 0;
//        /* here, we're using a for loop to add each element (using indexes) of the array into
//        the total */
//        for (int sus = 0; sus < numArray.length; sus++) {
//            total += numArray[sus];
//        }
//        System.out.println("The sum of 2, plus 3, plus 4, plus 5, plus 7, plus 8 is:");
//        System.out.println(total);

        // PROBLEM 2 - WHAT IS THE OUTPUT?
        /* There are so many problems with the source code, it ends Java refusing to run due
        to syntax errors, after corrections..
        the output ends up as: "2"
        */
//        double[ ] exampleArray = {1,5,6,5,4,1};
//        double maximum = exampleArray[0];
//        int index = 0;
//        for (int i = 1; i<exampleArray.length; i++){
//            if (exampleArray[ i ] > maximum) {
//                maximum = exampleArray[ i ];
//                index = i;
//            }
//        }
//        System.out.println(index);

        // PROBLEM 3 - toPower
        /*
        3. Write a public static method called "toPower" that takes in as parameters two
        integers called size and power.
        The method should return an array of size "power" with each array index raised to the
        value of "power." So, for example, if we passed in "size = 4" and
        "power = 2" to the "toPower," the method should return the following result: [0,1,4,9].

        On "MainToPower" class
         */



    }
}
